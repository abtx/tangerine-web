---
enum size {
	small,
	large,
}
export interface Props {
	name: string;
	size?: size;
}

const { name } = Astro.props as Props;
---

<button class="py-3 px-4 bg-blue-600 text-white rounded-full text-lg">
	{name}
</button>
